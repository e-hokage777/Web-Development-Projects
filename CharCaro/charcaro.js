const options = {
    spinner: {
        size: 500,
        spinSpeed: 2
    },
    itemBox: {
        width: 100,
        height: 100
    }
}
let animeCharSpinner = new CircularSpinner("anime-char-spinner", options);